{
  "name": "sifhos-root",
  "version": "1.0.0",
  "description": "Ejecución simultánea de frontend y backend para SIFHOS",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
